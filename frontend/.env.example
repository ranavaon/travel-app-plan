VITE_API_URL=http://localhost:3001
# Optional: Sign in with Apple (Services ID). When set, "התחבר עם Apple" is shown on login/register.
# VITE_APPLE_CLIENT_ID=com.example.service
# Optional: Redirect URI for Apple (must match Apple Developer config). Defaults to current origin.
# VITE_APPLE_REDIRECT_URI=https://yourdomain.com
VITE_GOOGLE_CLIENT_ID=
