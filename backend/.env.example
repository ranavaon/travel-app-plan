PORT=3001
SQLITE_PATH=./data.sqlite
# Optional; default used in dev. Set in production.
JWT_SECRET=your-secret-here
# Required for Google sign-in (POST /api/auth/google). Omit to disable.
GOOGLE_CLIENT_ID=
# Required for Sign in with Apple (POST /api/auth/apple). Service ID from Apple Developer. Omit to disable.
APPLE_CLIENT_ID=
